#include "Squares.h"

void Squares::checkForOverlaps(const Matrix& m)
{

  // slow and not good. Use findOverlaps in future
  /*  for (auto& a : m_grv_a.m_grv) {
    for (auto& b : m_grv_b.m_grv) {
      size_t ovl = 0;
      for (auto& c : m.m_vec) { // loop each chr + inter
	for (auto& j : c) { // loop each event within chr (or within inter)
	  size_t ovl1 = a->getOverlaps(j.)
	}
      }

	
      }
    }
    }*/

}
